# Psycopg API について

2025.12.5  

## psycopg2 (バージョン 2.9.11) API 仕様一覧

`psycopg2` は、PostgreSQL用のPythonデータベースアダプタです。  
Python Database DB API 2.0 (PEP 249) に準拠しつつ、PostgreSQL特有の機能やパフォーマンス向上を目的とした多くの拡張機能を提供しています。  

- [`psycopg2` モジュールインターフェース](#psycopg2-モジュールインターフェース)：グローバルインターフェース
- [`Connection` クラス](#connection-クラス)：データベースへの接続を管理
- [`Cursor` クラス](#cursor-クラス)：SQLコマンドの実行と結果を処理
- [`Extensions` クラス](#extensions-クラス)：PEP 249の機能拡張
  - [クラス定義 (`Extensions` クラス)](#クラス定義-extensions-クラス)
  - [SQL適応プロトコルオブジェクト (`Extensions` クラス)](#sql適応プロトコルオブジェクト-extensions-クラス)
  - [データベース型キャスティング関数 (`Extensions` クラス)](#データベース型キャスティング関数-extensions-クラス)
  - [追加の例外 (`Extensions` クラス)](#追加の例外-extensions-クラス)
  - [コルーチンのサポート関数 (`Extensions` クラス)](#コルーチンのサポート関数-extensions-クラス)
  - [その他の関数 (`Extensions` クラス)](#その他の関数-extensions-クラス)
- [`Extras` クラス](#extras-クラス)：追加機能
  - [接続とカーソルのサブクラス (`Extras` クラス)](#接続とカーソルのサブクラス-extras-クラス)
  - [レプリケーションサポートオブジェクト (`Extras` クラス)](#レプリケーションサポートオブジェクト-extras-クラス)
  - [追加データ型 (`Extras` クラス)](#追加データ型-extras-クラス)
  - [高速実行ヘルパー (`Extra` クラス)](#高速実行ヘルパー-extra-クラス)
  - [コルーチンサポート (`Extra` クラス)](#コルーチンサポート-extra-クラス)
- [その他のクラス](#その他のクラス)
  - [SQLオブジェクト (psycopg2.sql) クラス](#sqlオブジェクト-psycopg2sql-クラス)
  - [Connections pooling (psycopg2.pool) クラス](#connections-pooling-psycopg2pool-クラス)

### `psycopg2` モジュールインターフェース

Python Database DB API 2.0 (PEP 249) 仕様で要求されるモジュールレベルのオブジェクト。  

#### 接続関数

| 関数名                           | 説明 | サポート |
| :------------------------------- | :--- | :------: |
| `psycopg2.connect(...)`          | データベースへの接続を確立し `Connection` オブジェクトを返す。DSN文字列またはキーワード引数で接続情報を指定する | 〇 |

#### 属性

| 属性名                       | 説明 | サポート |
| :--------------------------- | :--- | :------: |
| `psycopg2.apilevel`          | DB-API仕様のバージョン ("2.0") | 〇 |
| `psycopg2.threadsafety`      | ドライバがスレッドセーフティをどのように扱うかを示す整数 (`1`: スレッド間で接続を共有すべきではない) | 確認 |
| `psycopg2.paramstyle`        | SQLクエリ内でパラメータをどのようにプレースホルダとして使うかを示す文字列 (`pyformat` または `format`) | 確認 |
| `psycopg2.__libpq_version__` | このpsycopg2モジュールがコンパイルされた `libpq` ライブラリのバージョン | 〇 |

#### 例外クラス (PEP 249準拠)

| クラス名                     | 説明 | サポート |
| :--------------------------- | :--- | :------: |
| `psycopg2.Warning`           | データベースに関する警告のための例外クラス | 確認 |
| `psycopg2.Error`             | データベース操作全般のエラーのための例外クラス | 〇 |
| `psycopg2.InterfaceError`    | データベースインターフェース自体に関するエラーのための例外クラス。`Error` のサブクラス | 確認 |
| `psycopg2.DatabaseError`     | データベース自体に関するエラーのための例外クラス。`Error` のサブクラス | 確認 |
| `psycopg2.DataError`         | データベースで処理中のデータに関する問題。`DatabaseError` のサブクラス | 確認 |
| `psycopg2.OperationalError`  | データベースの動作環境に関するエラー。`DatabaseError` のサブクラス | 確認 |
| `psycopg2.IntegrityError`    | データベースの整合性に関するエラー。`DatabaseError` のサブクラス | 確認 |
| `psycopg2.InternalError`     | データベースの内部エラー。`DatabaseError` のサブクラス | 確認 |
| `psycopg2.ProgrammingError`  | プログラミングエラー。`DatabaseError` のサブクラス | 確認 |
| `psycopg2.NotSupportedError` | ドライバがサポートしない機能を使用しようとした場合のエラー。`DatabaseError` のサブクラス | 確認 |

#### 例外クラス (psycopg2 拡張機能)

| 属性名                          | 説明 | サポート |
| :------------------------------ | :--- | :------: |
| `psycopg2.Error.pgerror` (属性) | バックエンドから返されたエラーメッセージ | 確認 |
| `psycopg2.Error.pgcode` (属性)  | バックエンドから返されたエラーコード | 確認 |
| `psycopg2.Error.cursor` (属性)  | 例外が発生したカーソルオブジェクト | 確認 |
| `psycopg2.Error.diag` (属性)    | エラーに関する詳細情報を含む `Diagnostics` オブジェクト | 確認 |

#### 型オブジェクト (PEP 249準拠)

| オブジェクト名                        | 説明 | サポート |
| :------------------------------------ | :--- | :------: |
| `psycopg2.Date(year, month, day)`     | 日付値を保持するオブジェクト | 〇 |
| `psycopg2.Time(hour, minute, second)` | 時間値を保持するオブジェクト | 〇 |
| `psycopg2.Timestamp(year, month, day, hour, minute, second)` | タイムスタンプ値を保持するオブジェクト | 〇 |
| `psycopg2.DateFromTicks(ticks)`       | ティック値から日付値を保持するオブジェクト | 確認 |
| `psycopg2.TimeFromTicks(ticks)`       | ティック値から時間値を保持するオブジェクト | 確認 |
| `psycopg2.TimestampFromTicks(ticks)`  | ティック値からタイムスタンプ値を保持するオブジェクト | 確認 |
| `psycopg2.Binary(string)`             | バイナリ（長）文字列値を保持するオブジェクト | × |
| `psycopg2.STRING`                     | 文字列ベースの列を記述する型オブジェクト | 〇 |
| `psycopg2.BINARY`                     | バイナリ列を記述する型オブジェクト | × |
| `psycopg2.NUMBER`                     | 数値列を記述する型オブジェクト | 確認 |
| `psycopg2.DATETIME`                   | 日付/時刻列を記述する型オブジェクト | 〇 |
| `psycopg2.ROWID`                      | 「行ID」列を記述する型オブジェクト | 確認 |

---

### `Connection` クラス

PostgreSQLデータベースへの接続を管理するオブジェクト。トランザクション制御、カーソル作成などを行う。

#### PEP 249準拠 (`Connection` クラス)

| 名前                     | 説明 | サポート |
| :----------------------- | :--- | :------: |
| `cursor(...)`            | 接続を使用して新しいカーソルオブジェクトを返す | 〇 |
| `commit()`               | 保留中のトランザクションをデータベースにコミットする | 〇 |
| `rollback()`             | 保留中のトランザクションの開始までロールバックする | 〇 |
| `close()`                | 接続を直ちに閉じる | 〇 |

#### psycopg2 拡張機能 (`Connection` クラス)

| 名前                        | 説明 | サポート |
| :-------------------------- | :--- | :------: |
| `cancel()`                  | 現在のデータベース操作をキャンセルする | 確認 |
| `reset()`                   | 接続をデフォルトにリセットする | 確認 |
| `set_isolation_level(...)`  | 現在のセッションのトランザクション分離レベルを設定する | × |
| `lobject(...)`              | 新しいデータベースラージオブジェクトを `lobject` インスタンスとして返す | × |
| `poll()`                    | 非同期接続試行中、またはカーソルが非同期接続でクエリを実行している場合、ブロックしない場合に通信を進める | 確認 |
| `fileno()`                  | 接続の基盤となるファイルディスクリプタを返す | 確認 |
| `isexecuting()`             | 接続が非同期操作を実行中である場合にTrueを返す | 確認 |
| `get_native_connection()`   | `PyCapsule` オブジェクトにラップされた内部 `PGconn*` を返す | 確認 |
| `get_transaction_status()`  | 現在のセッションのトランザクションステータスを整数として返す | 確認 |
| `get_backend_pid()`         | 接続したバックエンドサーバープロセスのプロセスID (PID) を返す | 確認 |
| `get_parameter_status(...)` | サーバーの現在のパラメータ設定を検索する | 確認 |
| `get_dsn_parameters()`      | 接続の有効なdsnパラメータを辞書として取得する | 確認 |
| `xid(...)`                  | この接続の `tpc_*()` メソッドに渡す `Xid` インスタンスを返す | × |
| `tpc_begin(...)`            | 指定されたトランザクションID `xid` でTPCトランザクションを開始する | × |
| `tpc_prepare()`             | `tpc_begin()` で開始されたトランザクションの最初のフェーズを実行する | × |
| `tpc_commit(...)`           | 準備されたTPCトランザクションをコミットする | × |
| `tpc_rollback(...)`         | 準備されたTPCトランザクションをロールバックする | × |
| `tpc_recover()`             | 保留中のトランザクションを表す `Xid` のリストを返す | × |
| `closed` (属性)             | 接続が開いている場合は0、閉じているか破損している場合はゼロ以外の値を返す読み取り専用の整数属性 | 確認 |
| `dsn` (属性)                | 接続によって使用された接続文字列を含む読み取り専用文字列 | 確認 |
| `autocommit` (属性)         | Trueの場合すべてのステートメントが即座に影響を及ぼす。Falseの場合最初のコマンド実行で新しいトランザクションが開始される | 〇 |
| `isolation_level` (属性)    | 現在のセッションのトランザクション分離レベルを返したり設定したりする | × |
| `readonly` (属性)           | 現在のセッションの読み取り専用ステータスを返したり設定したりする | 確認 |
| `deferrable` (属性)         | 現在のセッションのdeferrableステータスを返したり設定したりする | 確認 |
| `encoding` (属性)           | クライアントエンコーディングを読み取ったり設定したりする | 確認 |
| `notices` (属性)            | セッション中にクライアントに送信されたすべてのデータベースメッセージを含むリスト | 確認 |
| `notifies` (属性)           | セッションによって受信された非同期通知を含む `Notify` オブジェクトのリスト | 確認 |
| `cursor_factory` (属性)     | `cursor()` メソッドでパラメータが指定されていない場合に使用されるデフォルトのカーソルファクトリ | 確認 |
| `info` (属性)               | ネイティブの `libpq` 接続に関する情報を公開する `ConnectionInfo` オブジェクト | 確認 |
| `status` (属性)             | 接続のステータスを表す読み取り専用の整数 | 確認 |
| `async` (属性)              | 接続が非同期である場合は1、それ以外の場合は0を返す読み取り専用属性 | 確認 |
| `async_` (属性)             | `async` のエイリアス。Pythonのバージョンで `async` がキーワードである場合に利用可能 | 確認 |
| `pgconn_ptr` (属性)         | 内部の `PGconn*` を整数として返す | 確認 |
| `protocol_version` (属性)   | 使用されているフロントエンド/バックエンドプロトコルを表す読み取り専用の整数 | 確認 |
| `server_version` (属性)     | バックエンドバージョンを表す読み取り専用の整数 | 確認 |
| `__enter__()` / `__exit__()` (特殊メソッド) | コンテキストマネージャとして使用し、`with` ブロックの終了時にコミットまたはロールバックを自動的に行う | 確認 |

---

### `Cursor` クラス

SQLコマンドの実行と結果を処理するためのオブジェクト。SQLクエリの実行、フェッチなどを行う。

#### PEP 249準拠 (`Cursor` クラス)

| 名前                     | 説明 | サポート |
| :----------------------- | :--- | :------: |
| `close()`                | カーソルを閉じる。カーソルはそれ以降使用できない | 〇 |
| `execute(...)`           | データベース操作（クエリまたはコマンド）を実行する | 〇 |
| `executemany(...)`       | すべてのパラメータのタプルまたはマッピングのシーケンスに対して、データベース操作（クエリまたはコマンド）を実行する | 〇 |
| `callproc(...)`          | 指定された名前で保存されているデータベースプロシージャを呼び出す | × |
| `setinputsizes(...)`     | DB API 2.0に準拠して公開されているメソッド。現在は何もしない | × |
| `fetchone()`             | クエリ結果セットの次の行を取得し、単一のタプルとして返す | 〇 |
| `fetchmany(...)`         | クエリ結果の次の行のセットを取得し、タプルのリストとして返す | 〇 |
| `fetchall()`             | クエリ結果の残りの全ての行を取得し、タプルのリストとして返す | 〇 |
| `setoutputsize(...)`     | DB API 2.0に準拠して公開されているメソッド。現在は何もしない | × |
| `description` (属性)     | クエリ結果の記述 | 確認 |
| `arraysize` (属性)       | `fetchmany()` で一度にフェッチする行数を指定する読み書き可能な属性 | 〇 |
| `rowcount` (属性)        | 最後の `execute*()` が生成した行数または影響を与えた行数を指定する読み取り専用属性 | 〇 |
| `rownumber` (属性)       | 結果セット内のカーソルの現在の0ベースのインデックス、またはインデックスが決定できない場合はNoneを提供する読み取り専用属性 | 〇 |
| `lastrowid` (属性)       | カーソルによって挿入された最後の行のOIDを提供する読み取り専用属性 | 確認 |

#### psycopg2 拡張機能 (`Cursor` クラス)

| 名前                     | 説明 | サポート |
| :----------------------- | :--- | :------: |
| `mogrify(...)`           | 引数バインディング後のクエリ文字列を返す | 確認 |
| `scroll(...)`            | `mode` に応じて結果セット内のカーソルを新しい位置にスクロールする | 確認 |
| `cast(...)`              | PostgreSQLの文字列表現からPythonオブジェクトに値を変換する | 確認 |
| `nextset()`              | PostgreSQLではこのメソッドはサポートされておらず、`NotSupportedError` 例外を発生させる | 確認 |
| `copy_from(...)`         | `COPY FROM STDIN` コマンドを使用して、ファイルライクオブジェクトからデータをテーブルに読み込む | 確認 |
| `copy_to(...)`           | `COPY TO STDOUT` コマンドを使用して、テーブルの内容をファイルライクオブジェクトに書き込む | 確認 |
| `copy_expert(...)`       | ユーザーが作成した `COPY` ステートメントを送信する | 確認 |
| `closed` (属性)          | カーソルが閉じているかどうか（True）または閉じられていないか（False）を指定する読み取り専用ブール属性 | 確認 |
| `connection` (属性)      | カーソルが作成された接続オブジェクトへの参照を返す読み取り専用属性 | 確認 |
| `name` (属性)            | `connection.cursor()` によって名前付きカーソルとして作成された場合、カーソルの名前を含む読み取り専用属性 | 確認 |
| `scrollable` (属性)      | 名前付きカーソルが `SCROLL` として宣言されているかどうかを指定する読み書き可能な属性 | 確認 |
| `withhold` (属性)        | 名前付きカーソルの存続期間が現在のトランザクションの外部に及ぶべきかどうかを指定する読み書き可能な属性 | 確認 |
| `itersize` (属性)        | 名前付きカーソルをイテレートする際に、各ネットワークラウンドトリップでバックエンドからフェッチする行数を指定する読み書き可能な属性 | 確認 |
| `query` (属性)           | バックエンドに送信された最後のクエリの本体（バウンド引数を含む）をバイト文字列として含む読み取り専用属性 | 確認 |
| `statusmessage` (属性)   | 最後のコマンドによって返されたメッセージを含む読み取り専用属性 | 確認 |
| `tzinfo_factory` (属性)  | `TIMESTAMP WITH TIME ZONE` などのデータ型を処理するために使用されるタイムゾーンファクトリ | 確認 |
| `pgresult_ptr` (属性)    | カーソルの内部 `PGresult*` を整数として返す | 確認 |
| `__iter__()` (特殊メソッド) | カーソルをイテレータとして使用し、結果セットのレコードをループ処理する | 確認 |

---

### `Extensions` クラス

Python Database DB API 2.0 で定義された最小限の機能セットを拡張（Extension）するいくつかのオブジェクトと関数。

#### クラス定義 (`Extensions` クラス)

| 名前                            | 説明 | サポート |
| :------------------------------ | :--- | :------: |
| `connection(...)` (クラス)      | サブクラス化を可能にするために `extensions` モジュールによって公開されている | 確認 |
| `cursor(...)` (クラス)          | サブクラス化を可能にするために `extensions` モジュールによって公開されている | 確認 |
| `lobject(...)` (クラス)         | PostgreSQLのラージオブジェクトのラッパー | × |
| `ConnectionInfo(...)` (クラス)  | ネイティブのPostgreSQLデータベース接続に関する詳細。`connection.info` 属性として公開される | 確認 |
| `Column(...)` (クラス)          | クエリ結果の1つの列の記述。`cursor.description` シーケンスの項目として公開される | 確認 |
| `Notify(...)` (クラス)          | バックエンドから受信した通知。`listening connection` の `notifies` メンバーで受信時に利用可能になる | 確認 |
| `Xid(...)` (クラス)             | Two-phase commitで使用されるトランザクション識別子 | × |
| `Diagnostics(...)` (クラス)     | データベースエラーレポートからの詳細 | 確認 |

#### SQL適応プロトコルオブジェクト (`Extensions` クラス)

| 名前                          | 説明 | サポート |
| :---------------------------- | :--- | :------: |
| `adapt(...)`                  | PythonオブジェクトのSQL表現を `ISQLQuote` として返す | 確認 |
| `register_adapter(...)`       | `class` のオブジェクトの新しいアダプタを登録する | 確認 |
| `ISQLQuote(...)` (クラス)     | SQL適応プロトコルを表す | 確認 |
| `AsIs(...)` (クラス)          | 文字列表現が既に有効なSQL表現であるオブジェクトに役立つ `ISQLQuote` プロトコルに準拠したアダプタ | 確認 |
| `QuotedString(...)` (クラス)  | 文字列のようなオブジェクトの `ISQLQuote` プロトコルに準拠したアダプタ | 確認 |
| `Binary(...)` (クラス)        | バイナリオブジェクトの `ISQLQuote` プロトコルに準拠したアダプタ | × |
| `Boolean` (クラス)            | 組み込みオブジェクト用の特殊なアダプタ | × |
| `Float` (クラス)              | 組み込みオブジェクト用の特殊なアダプタ | 確認 |
| `SQL_IN` (クラス)             | 組み込みオブジェクト用の特殊なアダプタ | 確認 |
| `DateFromPy` (クラス)         | Python `datetime` オブジェクト用の特殊なアダプタ | 確認 |
| `TimeFromPy` (クラス)         | Python `datetime` オブジェクト用の特殊なアダプタ | 確認 |
| `TimestampFromPy` (クラス)    | Python `datetime` オブジェクト用の特殊なアダプタ | 確認 |
| `IntervalFromPy` (クラス)     | Python `datetime` オブジェクト用の特殊なアダプタ | × |
| `adapters` (属性)             | 現在登録されているオブジェクトアダプタの辞書 | 確認 |

#### データベース型キャスティング関数 (`Extensions` クラス)

| 名前                   | 説明 | サポート |
| :--------------------- | :--- | :------: |
| `new_type(...)`        | PostgreSQL型からPythonオブジェクトに変換するための新しいタイプキャスターを作成する | 確認 |
| `new_array_type(...)`  | PostgreSQL配列型からPythonオブジェクトのリストに変換するための新しいタイプキャスターを作成する | 確認 |
| `register_type(...)`   | `new_type()` を使用して作成されたタイプキャスターを登録する | 確認 |
| `string_types` (属性)  | タイプキャスターのグローバルレジスタ | 確認 |
| `encodings` (属性)     | PostgreSQLエンコーディングからPythonエンコーディング名へのマッピング | 確認 |

#### 追加の例外 (`Extensions` クラス)

| 名前                              | 説明 | サポート |
| :-------------------------------- | :--- | :------: |
| `QueryCanceledError` (例外クラス) | SQLクエリキャンセルに関連するエラー。`OperationalError` のサブクラス | 確認 |
| `TransactionRollbackError` (例外クラス) | トランザクションロールバックを引き起こすエラー（デッドロックなど）。`OperationalError` のサブクラス | 確認 |

#### コルーチンのサポート関数 (`Extensions` クラス)

| 名前                      | 説明 | サポート |
| :------------------------ | :--- | :------: |
| `set_wait_callback(...)`  | データが利用可能になるまで待機するコールバック関数を登録する | 確認 |
| `get_wait_callback()`     | 現在登録されている待機コールバック関数を返す | 確認 |

#### その他の関数 (`Extensions` クラス)

| 名前                      | 説明 | サポート |
| :------------------------ | :--- | :------: |
| `libpq_version()`         | ロードされた `libpq` 動的ライブラリのバージョン番号を整数として返す | 確認 |
| `make_dsn(...)`           | 引数から有効な接続文字列を作成する | 確認 |
| `parse_dsn(...)`          | 接続文字列をキーワードと値の辞書に解析する | 確認 |
| `quote_ident(...)`        | PostgreSQLの引用符付けルールに従って識別子を引用符で囲む | 確認 |
| `encrypt_password(...)`   | PostgreSQLパスワードの暗号化された形式を返す | 確認 |

---

### `Extras` クラス

Python Database DB API 2.0 で定義された最小限の機能セットに追加（Extras）するいくつかのオブジェクトと関数。

#### 接続とカーソルのサブクラス (`Extras` クラス)

| 名前                                | 説明 | サポート |
| :---------------------------------- | :--- | :------: |
| `DictCursor(...)` (クラス)          | 列名とインデックスのマッピングのリストを保持するカーソル | × |
| `DictConnection` (クラス)           | `DictCursor` を自動的に使用する接続 | × |
| `DictRow(...)` (クラス)             | 列名によるデータアクセスを可能にする行オブジェクト | × |
| `RealDictCursor(...)` (クラス)      | 行の基本タイプとして実際の辞書を使用するカーソル | × |
| `RealDictConnection` (クラス)       | `RealDictCursor` を自動的に使用する接続 | × |
| `RealDictRow(...)` (クラス)         | データレコードを表す `dict` サブクラス | × |
| `NamedTupleCursor` (クラス)         | 結果を `namedtuple` として生成するカーソル | × |
| `NamedTupleConnection` (クラス)     | `NamedTupleCursor` を自動的に使用する接続 | × |
| `LoggingConnection` (クラス)        | 全てのクエリをファイルまたはロガーオブジェクトに記録する接続 | × |
| `LoggingCursor` (クラス)            | 接続のロギング機能を使用してクエリを記録するカーソル | × |
| `MinTimeLoggingConnection` (クラス) | 実行時間に基づいてクエリを記録する接続 | × |
| `MinTimeLoggingCursor` (クラス)     | `MinTimeLoggingConnection` のコンパニオンカーソル | × |

#### レプリケーションサポートオブジェクト (`Extras` クラス)

| 名前                                | 説明 | サポート |
| :---------------------------------- | :--- | :------: |
| `REPLICATION_LOGICAL` (定数)        | 論理レプリケーションタイプ | × |
| `REPLICATION_PHYSICAL` (定数)       | 物理レプリケーションタイプ | × |
| `LogicalReplicationConnection(...)` (クラス) | 論理レプリケーションに使用される特別な種類の接続を開くために使用できる接続ファクトリクラス | × |
| `PhysicalReplicationConnection(...)` (クラス) | 物理レプリケーションに使用される特別な種類の接続を開くために使用できる接続ファクトリクラス | × |
| `ReplicationMessage` (クラス)       | レプリケーションプロトコルメッセージ | × |
| `ReplicationCursor` (クラス)        | レプリケーション接続で通信するために使用されるカーソル | × |
| `StopReplication` (例外クラス)      | `consume_stream()` 内の無限ループから抜け出すために使用される例外。`Exception` のサブクラス | × |

#### 追加データ型 (`Extras` クラス)

| 名前                                | 説明 | サポート |
| :---------------------------------- | :--- | :------: |
| `Json(...)` (クラス)                | Pythonオブジェクトを `json` データ型に適応させる `ISQLQuote` ラッパー | × |
| `register_json(...)`                | `json` 型をPythonオブジェクトに変換するタイプキャスターを作成および登録する | × |
| `register_default_json(...)`        | `json` 型用のタイプキャスターを作成および登録する | × |
| `register_default_jsonb(...)`       | `jsonb` 型用のタイプキャスターを作成および登録する | × |
| `register_hstore(...)`              | `dict` と `hstore` 間の変換用アダプタとタイプキャスターを登録する | × |
| `register_composite(...)`           | 複合型をタプルに変換するタイプキャスターを登録する | × |
| `CompositeCaster(...)` (クラス)     | PostgreSQL複合型をPythonオブジェクトに変換するのに役立つクラス | × |
| `Range(...)` (クラス)               | PostgreSQLの範囲型を表すPython表現 | × |
| `NumericRange(...)` (クラス)        | Pythonの数値型をPostgreSQLの範囲に渡すのに適した `Range` | × |
| `DateRange(...)` (クラス)           | `daterange` 値を表す `Range` | × |
| `DateTimeRange(...)` (クラス)       | `tsrange` 値を表す `Range` | × |
| `DateTimeTZRange(...)` (クラス)     | `tstzrange` 値を表す `Range` | × |
| `register_range(...)`               | PostgreSQL範囲型と `Range` サブクラス間の変換用アダプタとタイプキャスターを作成および登録する | × |
| `RangeCaster(...)` (クラス)         | `Range` とPostgreSQL範囲型間の変換を支援するクラス | × |
| `register_uuid(...)`                | `UUID` 型と `uuid.UUID` アダプタを作成する | × |
| `UUID_adapter(...)` (クラス)        | Pythonの `uuid.UUID` 型をPostgreSQLの `uuid` に適応させる | × |
| `register_ipaddress(...)`           | `ipaddress` オブジェクトとネットワーク型間の変換サポートを登録する | × |
| `register_inet(...)`                | `INET` 型と `Inet` アダプタを作成する（非推奨） | × |
| `Inet(...)` (クラス)                | `inet` 値の正しいSQL引用符付けを可能にするために文字列をラップする（非推奨） | × |

#### 高速実行ヘルパー (`Extra` クラス)

| 名前                                | 説明 | サポート |
| :---------------------------------- | :--- | :------: |
| `execute_batch(...)`                | 複数のステートメントを少ないサーバーラウンドトリップで実行する | × |
| `execute_values(...)`               | `VALUES` を使用して、パラメータのシーケンスでステートメントを実行する | × |

#### コルーチンサポート (`Extra` クラス)

| 名前                                | 説明 | サポート |
| :---------------------------------- | :--- | :------: |
| `wait_select(...)`                  | 接続またはカーソルがデータを利用可能になるまで待機する | × |

---

### その他のクラス

#### SQLオブジェクト (psycopg2.sql) クラス

SQL識別子（テーブル名やフィールド名など）を生成するのに役立つオブジェクト。

| 名前                            | 説明 | サポート |
| :------------------------------ | :--- | :------: |
| `Composable(...)` (抽象クラス)  | SQL文字列を合成するために使用できるオブジェクトの抽象基底クラス | 確認 |
| `SQL(...)` (クラス)             | SQLステートメントのスニペットを表す `Composable` | 〇 |
| `Identifier(...)` (クラス)      | SQL識別子またはドット区切りのシーケンスを表す `Composable` | 確認 |
| `Literal(...)` (クラス)         | クエリに含めるSQL値を表す `Composable` | 確認 |
| `Placeholder(...)` (クラス)     | クエリパラメータのプレースホルダを表す `Composable` | 〇 |
| `Composed(...)` (クラス)        | `Composable` のシーケンスで構成される `Composable` オブジェクト | 確認 |

#### Connections pooling (psycopg2.pool) クラス

接続を管理し、接続を必要とする関数でそれらの接続を使用できるようにするオブジェクト

| 名前                                   | 説明 | サポート |
| :------------------------------------- | :--- | :------: |
| `AbstractConnectionPool(...)` (クラス) | 汎用的なキーベースのプーリングコードを実装する基底クラス | 確認 |
| `SimpleConnectionPool(...)` (クラス)   | 異なるスレッド間で共有できない接続プール | 確認 |
| `ThreadedConnectionPool(...)` (クラス) | `threading` モジュールで動作する接続プール | 確認 |

以上
