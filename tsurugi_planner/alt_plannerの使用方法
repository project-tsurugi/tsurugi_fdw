■alt_plannerの使い方

1. 以下のコマンドを発行する
   make
   make install
   
2. postgresql.confのshared_preload_librariesを以下のように設定する

     shared_preload_libraries = 'alt_planner'
     
   ※postgresql.confは、<PostgreSQLインストールディレクトリ>/data/ に存在します。

3. データベースクラスタを再起動する

     pg_ctl stop
     pg_ctl start